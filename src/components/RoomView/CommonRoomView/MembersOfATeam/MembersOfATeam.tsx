import React from "react";
import MembersOfATeamView from "./MembersOfATeam.view";

const MembersOfATeam: React.FC<any> = (props) => {
  return <MembersOfATeamView {...props} />;
};

export default MembersOfATeam;
